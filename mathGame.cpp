#include "keyPair.h"
#include <iostream>
#include <string>

const int noOfValues = 10;

void generateTable() {		
	int(*fnTable[noOfValues])(int) = {}; // Generate array of pointers to Functions containing our usable functions.
}

int main() {

	return 0;
}